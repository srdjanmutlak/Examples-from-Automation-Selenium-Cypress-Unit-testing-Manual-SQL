describe('Our first test', () => {
    it('should confirm that 1 + 1 equals 2', () => {
        // These are Explicit Assertions
        expect(1 + 1).to.equal(2);

        //expect(true).to.be.true
    })
})